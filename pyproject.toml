[project]
name = "claimguard_uipath_agent"
version = "0.0.3"
description = "ClaimGuard AI deterministic insurance claims orchestration coded agent"
authors = [{ name = "ClaimGuard AI", email = "demo@example.com" }]
dependencies = [
    "uipath>=2.11.0, <3.0.0"
]
requires-python = ">=3.11"
